node_modules
dist
dev-dist
.env

# Generated archives must never be committed (one-off PWA-asset exports, etc.).
*.zip

# Legacy dev service-worker output (no longer generated) — ignored so a leftover artifact
# from an older checkout never gets committed. The production SW is emitted into dist/ by
# vite-plugin-pwa's injectManifest.
public/claims-sw.js
public/claims-sw.js.map

# Self-hosted OCR (tesseract.js) assets — vendored into public/tessdata/ by scripts/copy-tessdata.mjs
# (prebuild). Large binaries regenerated from node_modules + CDN, so never committed.
public/tessdata/
